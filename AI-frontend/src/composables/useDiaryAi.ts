/** AI 集成占位，Phase 3 实现 */
export const isDiaryAiEnabled = false

export function openDiaryAiPanel(_context?: { entryId?: number; date?: string }) {
  // Phase 3: 打开 AI 侧栏或跳转 /chat?from=diary
}

export function insertFromChat(_text: string) {
  // Phase 3: 从对话写回日记正文
}
