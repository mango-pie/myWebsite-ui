/**
 * 日记 API 别名 — 实际由 openapi2ts 生成于 diaryEntryController.ts
 * 保留 diaryController 命名以兼容旧 import，且不受 openapi2ts 覆盖。
 */
export {
  deleteDiaryEntry,
  getDiaryByDate,
  getDiaryEntryVo,
  getDiaryPrevNext,
  listDiaryByMonth,
  queryDiaryPage,
  saveDiaryEntry,
} from '@/api/diaryEntryController'
